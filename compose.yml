services:
  web:
    build: .
    command: bash
    volumes:
      - .:/app
    ports:
      - "3002:3000"
    depends_on:
      - db
    
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password